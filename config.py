# List of drives to search
SEARCH_DRIVES = ["C:", "D:", "E:"]

# File extensions to ignore for faster search
IGNORE_EXTENSIONS = [".tmp", ".dll", ".sys", ".bin"]

# Directories to skip for faster search
SKIP_DIRECTORIES = ["$Recycle.Bin", "ProgramData"]